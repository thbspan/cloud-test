spring:
  application:
    name: demo-consumer
server:
  port: 48080
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka:eureka@localhost:8761/eureka/
